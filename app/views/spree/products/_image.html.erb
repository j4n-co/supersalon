<% if image %> 
  <%= image_tag image.attachment.url(:original), :itemprop => "image", :id=>"product-image" %>
<% else %>
  <%= large_image(@product, :itemprop => "image",  :id=>"product-image" ) %>
<% end %>