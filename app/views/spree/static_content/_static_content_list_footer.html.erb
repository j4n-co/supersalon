<% if page.foreign_link.present? %>
  <li><%= link_to page.title, page.foreign_link, {:target => "_blank"} %></li>
<% else %>
  <% page_uri = Rails.application.routes.named_routes[:spree].path.spec.to_s + page.slug %>
  <li><%= link_to page.title, page_uri  %></li>
<% end %>

