#
# Format:
#
# <stage name>:
#   app: <Heroku app name>
#   stack: <Heroku stack, optional>
#   tag: <git tag pattern, optional>
#   repo: <git repository, optional>
#   config:
#     - <Heroku config:var name>: <Heroku config:var value>
#

staging:
  stack: red 
  app: morning-depths-2512
  config:
    BUNDLE_WITHOUT: "development:test:production"