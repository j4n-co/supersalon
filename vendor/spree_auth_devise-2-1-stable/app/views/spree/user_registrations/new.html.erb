<% @body_id = 'signup' %>

<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @user } %>
<div id="new-customer">
  <h1 style="text-align:center;"><%= Spree.t(:new_customer) %></h1>

  <div data-hook="signup" style="text-align:center;">
    <%= form_for resource, :as => :spree_user, :url => spree.registration_path(@user) do |f| %>
      <div data-hook="signup_inside_form">
        <%= render :partial => 'spree/shared/user_form', :locals => { :f => f } %>
        <p style="margin-top: 2em;"><%= f.submit Spree.t(:create_a_new_account), :class => 'button primary' %></p>
      </div>
    <% end %>
  </div>

</div>

<div data-hook="login_extras"></div>
